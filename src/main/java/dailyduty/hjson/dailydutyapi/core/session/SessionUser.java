package dailyduty.hjson.dailydutyapi.core.session;
/**
 *  SessionUser
 *
 *  key, value만 있으면 된다.
 * 로그인시 정보를 저장한 user정보 dto로 지정. (session에 유저정보를 저장할것)
 * @Gettr만씀.
 * //board에 들어오는 유저마다 먼저 session키가 있는지 부터본다.
 */
public class SessionUser {
}
